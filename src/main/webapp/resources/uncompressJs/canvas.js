$().ready(function(){
	$background = '<div class="content">'
				+ '<div id="large-header" class="large-header">'
				+ '<canvas id="demo-canvas"></canvas>'
				+ '</div>'
				+ '</div>';
	$('body').append($background);
});